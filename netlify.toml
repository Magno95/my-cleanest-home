[build]
  base = "."
  command = "pnpm --filter @mch/web build"
  publish = "apps/web/dist"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "9.0.5"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
